# ============================================================
# Quid MCP Configuration
# ============================================================

# Backend API Configuration
# Port for the backend server (default: 91060 if not specified)
PORT=91060
# Host to bind to (default: 0.0.0.0 for all interfaces, use 127.0.0.1 for localhost only)
HOST=0.0.0.0
BACKEND_URL=http://localhost:91060

# Optional: API Key for authentication
# API_KEY=your_secret_api_key_here

# Optional: CORS Origins (comma-separated)
# CORS_ORIGINS=*

# Browser Settings (Optional)
# HEADLESS=true
# PLAYWRIGHT_SLOW_MO=500
# PLAYWRIGHT_KEEP_OPEN=false

# ============================================================
# Plugin Credentials
# ============================================================
# Set credentials for each plugin you use
# Format: {PLUGIN_ID}_EMAIL and {PLUGIN_ID}_PASSWORD
#         or {PLUGIN_ID}_API_KEY

# EMIS Plugin (environmental data - Belgium)
EMIS_URL=https://navigator.emis.vito.be
EMIS_EMAIL=your_email@example.com
EMIS_PASSWORD=your_password

# Example: Add credentials for other plugins
# MEDICINES_UK_API_KEY=your_api_key
# STAATSBLAD_EMAIL=your_email@example.com
# STAATSBLAD_PASSWORD=your_password

# ============================================================
# Advanced Configuration (Optional)
# ============================================================

# Session file directory
# SESSION_DIR=data/sessions

# Log level
# LOG_LEVEL=INFO
