# EMIS Portal Credentials (REQUIRED)
# If not set, will use hardcoded fallback for local development
EMIS_EMAIL=your_email@example.com
EMIS_PASSWORD=your_password

# EMIS Portal URLs (optional - defaults shown)
# Base URL for EMIS portal
EMIS_BASE_URL=https://emis.vito.be
# Direct login URL for EMIS navigator
EMIS_LOGIN_URL=https://navigator.ems.vito.be

# API Configuration
PORT=38153

# Backend API URL (for skill script)
BACKEND_URL=http://localhost:38153

# Optional: API Key for authentication (if set, /query endpoint requires X-API-Key header)
# API_KEY=your_secret_api_key_here

# Optional: CORS Origins (comma-separated, defaults to "*" for development)
# Set to "*" or leave empty to allow all origins (local development)
# For production, specify exact origins: http://yourdomain.com,https://yourdomain.com
# CORS_ORIGINS=*

# Optional: Browser visibility settings
# HEADLESS=false              # Browser visible (default: false)
# PLAYWRIGHT_SLOW_MO=500      # Delay between actions in ms (default: 500)
# PLAYWRIGHT_KEEP_OPEN=false  # Keep browser open after completion (default: false)

# Optional: Session file path (defaults to data/session.json)
# SESSION_FILE=data/session.json
